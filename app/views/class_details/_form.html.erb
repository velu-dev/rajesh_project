<%= form_with(model: class_detail, local: true) do |form| %>
  <% if class_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(class_detail.errors.count, "error") %> prohibited this class_detail from being saved:</h2>

      <ul>
      <% class_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :class_name %>
    <%= form.text_field :class_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
